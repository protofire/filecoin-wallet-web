import type { Palette } from '@mui/material'

const NETWORK_PALETTE = {
  secondary: {
    dark: '#0069BA',
    main: '#0090FF',
    light: '#39C1CB',
    background: '#EFFFF4',
  },
} as Partial<Palette>
export default NETWORK_PALETTE
